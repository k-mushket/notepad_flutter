import 'package:flutter/material.dart';

class NotepadCheck extends StatelessWidget {
  const NotepadCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}